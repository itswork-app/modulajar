# Example Environment Variables
# CLERK_ISSUER=
GCS_BUCKET=modulajar-artifacts

# PR-016: Service Split
# SERVICE_MODE: api (default) or verify
SERVICE_MODE=api
VERIFY_BASE_URL=https://verify.modulajar.app
PORT=3000
HOST=0.0.0.0
